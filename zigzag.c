#include <stdio.h>
#include <stdlib.h>
#include <string.h>


char* convert(char* in_str, int numRows)
{
    int nlen = strlen(in_str);
    int num_per_bach = numRows * 2 -2;
    char* rt_str = (char*)malloc(sizeof(char)* (nlen + 1));
    int index = 0;
    for(int row = 0; row < numRows; ++row)
    {
        
    }
}